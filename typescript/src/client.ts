/**
 * AIoIA Core - Client-side only exports
 *
 * This module contains all code that must run on the client side.
 * Import from '@aioia/core/client' to use these in your application.
 *
 * The 'use client' directive ensures this code is properly bundled
 * for client-side execution in React Server Components (RSC) environments.
 */
"use client";

// Components
export { LottiePlayer } from "./components/LottiePlayer";
export type { LottiePlayerProps } from "./components/LottiePlayer";
